# Copy this file to .env for local development. Do not commit .env.
SECRET_KEY=replace-with-a-long-random-secret
MONGODB_URL=mongodb://localhost:27017
FASTAPI_DATABASE_NAME=ERP
FASTAPI_PORT=8000
FILE_PATH=uploads
USERS_COLLECTION=users
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
AUTH_USERNAME=
AUTH_PASSWORD=
# Test credentials are intentionally empty. Use the supplied Staff account
# locally, and configure the same values as GitHub repository secrets.
TEST_STAFF_EMAIL=admin@gmail.com
TEST_STAFF_PASSWORD=
HOST=localhost

